<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Contact - URL Shortener</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        :root {
            --primary: #e60012;
            --signal: #f68d1f;
            --amber: #ecab37;
            --canvas: #7a8aba;
            --canvas-soft: #9fbee7;
            --periwinkle: #8ba1d4;
            --chrome-indigo: #3d4f97;
            --platinum: #dedede;
            --surface: #ffffff;
            --carbon: #21242e;
            --ink: #21242e;
            --ink-soft: #3d4f97;
            --shadow: rgba(33, 36, 46, 0.22);
        }

        * { box-sizing: border-box; }

        body {
            margin: 0;
            min-height: 100vh;
            font-family: Arial, Helvetica, sans-serif;
            color: var(--ink);
            background:
                radial-gradient(circle at top left, rgba(255, 255, 255, 0.34), transparent 35%),
                linear-gradient(180deg, #9cb2dc 0%, var(--canvas) 22%, #8d9fcd 58%, #7387b8 100%);
        }

        a { color: inherit; text-decoration: none; }

        .shell {
            width: min(960px, calc(100% - 24px));
            margin: 0 auto;
            padding: 12px 0 20px;
        }

        .topbar {
            background: var(--carbon);
            color: #fff;
            border: 1px solid rgba(255,255,255,0.08);
            box-shadow: 0 10px 24px var(--shadow);
            padding: 10px 14px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 14px;
            flex-wrap: wrap;
        }

        .brand-badge {
            background: var(--surface);
            color: var(--primary);
            border-radius: 9999px;
            padding: 8px 14px;
            font-weight: 900;
            letter-spacing: 0.08em;
            text-transform: uppercase;
        }

        .nav {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            color: var(--amber);
        }

        .subbar {
            background: linear-gradient(180deg, #b0c8eb, var(--canvas-soft));
            color: var(--ink);
            border-left: 1px solid rgba(255,255,255,0.4);
            border-right: 1px solid rgba(33, 36, 46, 0.15);
            border-bottom: 1px solid rgba(61, 79, 151, 0.4);
            padding: 8px 14px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
        }

        .hero {
            margin-top: 16px;
            background: linear-gradient(180deg, rgba(255,255,255,0.12), rgba(255,255,255,0.02)), linear-gradient(180deg, #aab9e4, #9eaee0);
            border: 1px solid rgba(61, 79, 151, 0.5);
            box-shadow: 0 14px 30px var(--shadow);
            padding: 22px;
        }

        .eyebrow {
            display: inline-flex;
            background: var(--carbon);
            color: var(--canvas-soft);
            padding: 5px 10px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        h1 {
            margin: 14px 0 8px;
            font-size: clamp(2rem, 4vw, 3.4rem);
            line-height: 0.95;
            text-transform: uppercase;
            color: #fff;
            text-shadow:
                -3px -3px 0 var(--carbon),
                3px -3px 0 var(--carbon),
                -3px 3px 0 var(--carbon),
                3px 3px 0 var(--carbon),
                0 6px 0 rgba(33, 36, 46, 0.55);
        }

        .lede {
            margin: 0;
            max-width: 64ch;
            font-size: 14px;
            line-height: 1.55;
            color: #24304b;
        }

        .content {
            margin-top: 18px;
            display: grid;
            grid-template-columns: 1fr 0.9fr;
            gap: 14px;
        }

        .panel {
            background: rgba(255,255,255,0.92);
            border: 1px solid rgba(61, 79, 151, 0.28);
            border-radius: 4px;
            padding: 18px;
            box-shadow: inset 0 1px 0 rgba(255,255,255,0.85);
        }

        .panel h2 {
            margin: 0 0 12px;
            font-size: 16px;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        .panel p {
            margin: 0;
            line-height: 1.55;
            color: #394968;
            font-size: 13px;
        }

        .list {
            display: grid;
            gap: 10px;
            margin-top: 14px;
        }

        .item {
            display: flex;
            gap: 12px;
            align-items: flex-start;
            background: linear-gradient(180deg, #fff, #eef2fb);
            border: 1px solid rgba(61, 79, 151, 0.22);
            border-radius: 4px;
            padding: 12px;
        }

        .item .badge {
            flex: 0 0 auto;
            background: var(--signal);
            color: #fff;
            border-radius: 9999px;
            min-width: 30px;
            height: 30px;
            display: grid;
            place-items: center;
            font-weight: 700;
            font-size: 12px;
        }

        .item strong {
            display: block;
            margin-bottom: 4px;
            text-transform: uppercase;
            letter-spacing: 0.06em;
            color: var(--carbon);
            font-size: 12px;
        }

        .item span {
            display: block;
            font-size: 12px;
            line-height: 1.45;
            color: #394968;
        }

        form {
            display: grid;
            gap: 12px;
        }

        input,
        textarea {
            width: 100%;
            border: 1px solid rgba(61, 79, 151, 0.45);
            border-top-color: rgba(255,255,255,0.9);
            background: #fff;
            border-radius: 4px;
            padding: 13px 14px;
            font: inherit;
            font-size: 14px;
            color: var(--ink);
            box-shadow: inset 0 1px 2px rgba(33, 36, 46, 0.08);
        }

        textarea {
            resize: vertical;
            min-height: 140px;
        }

        input:focus,
        textarea:focus {
            outline: 3px solid rgba(246, 141, 31, 0.28);
            border-color: var(--signal);
        }

        button {
            border: 0;
            border-radius: 4px;
            cursor: pointer;
            font: inherit;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            background: linear-gradient(180deg, #f3be53, var(--amber));
            color: var(--carbon);
            padding: 14px 18px;
            box-shadow: inset 0 1px 0 rgba(255,255,255,0.7), 0 6px 0 rgba(138, 98, 12, 0.24);
        }

        button:hover {
            transform: translateY(-1px);
        }

        .secondary {
            margin-top: 14px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: var(--carbon);
            color: #fff;
            padding: 12px 16px;
            border-radius: 4px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        footer {
            margin-top: 16px;
            background: var(--carbon);
            color: var(--canvas-soft);
            font-size: 10px;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            padding: 14px 16px;
            box-shadow: 0 10px 24px var(--shadow);
        }

        @media (max-width: 820px) {
            .content {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="shell">
        <header class="topbar">
            <div class="brand-badge">URL Shortener</div>
            <nav class="nav" aria-label="Primary">
                <a href="/index">Home</a>
                <a href="/about">About</a>
                <a href="/contact">Contact</a>
            </nav>
        </header>

        <div class="subbar">
            <span>Reach out</span>
            <span>Ask about the server</span>
            <span>Send feedback</span>
        </div>

        <section class="hero">
            <div class="eyebrow">Support panel</div>
            <h1>Contact the project</h1>
            <p class="lede">Use this form to send a message about the server, suggest improvements, or ask questions about how the shortening flow works.</p>

            <div class="content">
                <div class="panel">
                    <h2>What this page is for</h2>
                    <p>The form posts directly to the Rust server, which stores the submission as JSON for later review. The styling matches the home and result pages so the whole site feels like one system.</p>

                    <div class="list">
                        <div class="item">
                            <div class="badge">1</div>
                            <div><strong>Leave a note</strong><span>Tell us what part of the app you want improved or where the flow feels rough.</span></div>
                        </div>
                        <div class="item">
                            <div class="badge">2</div>
                            <div><strong>Share context</strong><span>Include the destination or use case if you need help debugging a specific redirect.</span></div>
                        </div>
                        <div class="item">
                            <div class="badge">3</div>
                            <div><strong>Keep moving</strong><span>The primary link-shortening action is only one click away from the home page.</span></div>
                        </div>
                    </div>
                </div>

                <div class="panel">
                    <h2>Send a message</h2>
                    <form method="POST" action="/contact">
                        <input type="text" name="name" placeholder="Your name" required />
                        <input type="email" name="email" placeholder="Your email" required />
                        <textarea name="message" rows="5" placeholder="Your message" required></textarea>
                        <button type="submit">Send Message</button>
                    </form>
                    <a class="secondary" href="/index">Back to Home</a>
                </div>
            </div>
        </section>

        <footer>
            &copy; 2026 URL Shortener. Contact form powered by the same Rust server.
        </footer>
    </div>
</body>
</html>
