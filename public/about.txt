<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>About - URL Shortener</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        :root {
            --primary: #e60012;
            --signal: #f68d1f;
            --amber: #ecab37;
            --canvas: #7a8aba;
            --canvas-soft: #9fbee7;
            --periwinkle: #8ba1d4;
            --chrome-indigo: #3d4f97;
            --platinum: #dedede;
            --surface: #ffffff;
            --carbon: #21242e;
            --ink: #21242e;
            --ink-soft: #3d4f97;
            --shadow: rgba(33, 36, 46, 0.22);
        }

        * { box-sizing: border-box; }

        body {
            margin: 0;
            min-height: 100vh;
            font-family: Arial, Helvetica, sans-serif;
            color: var(--ink);
            background:
                radial-gradient(circle at top left, rgba(255, 255, 255, 0.34), transparent 35%),
                linear-gradient(180deg, #9cb2dc 0%, var(--canvas) 22%, #8d9fcd 58%, #7387b8 100%);
        }

        a { color: inherit; text-decoration: none; }

        .shell {
            width: min(960px, calc(100% - 24px));
            margin: 0 auto;
            padding: 12px 0 20px;
        }

        .topbar {
            background: var(--carbon);
            color: #fff;
            border: 1px solid rgba(255,255,255,0.08);
            box-shadow: 0 10px 24px var(--shadow);
            padding: 10px 14px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 14px;
            flex-wrap: wrap;
        }

        .brand-badge {
            background: var(--surface);
            color: var(--primary);
            border-radius: 9999px;
            padding: 8px 14px;
            font-weight: 900;
            letter-spacing: 0.08em;
            text-transform: uppercase;
        }

        .nav {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            color: var(--amber);
        }

        .subbar {
            background: linear-gradient(180deg, #b0c8eb, var(--canvas-soft));
            color: var(--ink);
            border-left: 1px solid rgba(255,255,255,0.4);
            border-right: 1px solid rgba(33, 36, 46, 0.15);
            border-bottom: 1px solid rgba(61, 79, 151, 0.4);
            padding: 8px 14px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
        }

        .hero {
            margin-top: 16px;
            background: linear-gradient(180deg, rgba(255,255,255,0.12), rgba(255,255,255,0.02)), linear-gradient(180deg, #aab9e4, #9eaee0);
            border: 1px solid rgba(61, 79, 151, 0.5);
            box-shadow: 0 14px 30px var(--shadow);
            padding: 22px;
        }

        .eyebrow {
            display: inline-flex;
            background: var(--carbon);
            color: var(--canvas-soft);
            padding: 5px 10px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        h1 {
            margin: 14px 0 8px;
            font-size: clamp(2rem, 4vw, 3.4rem);
            line-height: 0.95;
            text-transform: uppercase;
            color: #fff;
            text-shadow:
                -3px -3px 0 var(--carbon),
                3px -3px 0 var(--carbon),
                -3px 3px 0 var(--carbon),
                3px 3px 0 var(--carbon),
                0 6px 0 rgba(33, 36, 46, 0.55);
        }

        .lede {
            margin: 0;
            max-width: 64ch;
            font-size: 14px;
            line-height: 1.55;
            color: #24304b;
        }

        .content {
            margin-top: 18px;
            display: grid;
            grid-template-columns: 1.1fr 0.9fr;
            gap: 14px;
        }

        .panel {
            background: rgba(255,255,255,0.92);
            border: 1px solid rgba(61, 79, 151, 0.28);
            border-radius: 4px;
            padding: 18px;
            box-shadow: inset 0 1px 0 rgba(255,255,255,0.85);
        }

        .panel h2 {
            margin: 0 0 12px;
            font-size: 16px;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        .panel p,
        .panel li {
            line-height: 1.55;
            color: #394968;
            font-size: 13px;
        }

        ul {
            margin: 12px 0 0;
            padding-left: 18px;
        }

        .stack {
            display: grid;
            gap: 10px;
        }

        .metric {
            background: linear-gradient(180deg, #fff, #eef2fb);
            border: 1px solid rgba(61, 79, 151, 0.22);
            border-radius: 4px;
            padding: 12px;
        }

        .metric strong {
            display: block;
            color: var(--primary);
            font-size: 20px;
            line-height: 1;
        }

        .metric span {
            display: block;
            margin-top: 6px;
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            font-weight: 700;
            color: var(--ink-soft);
        }

        .secondary {
            margin-top: 14px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(180deg, #f3be53, var(--amber));
            color: var(--carbon);
            padding: 12px 16px;
            border-radius: 4px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        .secondary:hover {
            transform: translateY(-1px);
        }

        footer {
            margin-top: 16px;
            background: var(--carbon);
            color: var(--canvas-soft);
            font-size: 10px;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            padding: 14px 16px;
            box-shadow: 0 10px 24px var(--shadow);
        }

        @media (max-width: 820px) {
            .content {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="shell">
        <header class="topbar">
            <div class="brand-badge">URL Shortener</div>
            <nav class="nav" aria-label="Primary">
                <a href="/index">Home</a>
                <a href="/about">About</a>
                <a href="/contact">Contact</a>
            </nav>
        </header>

        <div class="subbar">
            <span>Low overhead</span>
            <span>Rust networking</span>
            <span>Custom HTML pages</span>
        </div>

        <section class="hero">
            <div class="eyebrow">About the project</div>
            <h1>Built as a compact Rust web server</h1>
            <p class="lede">This project is a simple URL shortener that handles HTTP requests directly with Rust's standard networking primitives, stores mappings in JSON, and serves a chrome-inspired interface for a more polished experience.</p>

            <div class="content">
                <div class="panel">
                    <h2>What it does</h2>
                    <p>The application accepts a long URL, generates a compact short code, stores the mapping persistently, and redirects anyone who visits the short link back to the original destination.</p>
                    <ul>
                        <li>Shortens URLs into stable, shareable codes.</li>
                        <li>Redirects short links back to the saved destination.</li>
                        <li>Persists data in JSON for simple local storage.</li>
                        <li>Serves browser pages directly from the Rust server.</li>
                    </ul>
                    <a class="secondary" href="/index">Try the home page</a>
                </div>

                <div class="panel">
                    <h2>Why the redesign</h2>
                    <div class="stack">
                        <div class="metric">
                            <strong>01</strong>
                            <span>Sharper hierarchy</span>
                        </div>
                        <div class="metric">
                            <strong>02</strong>
                            <span>Branded chrome palette</span>
                        </div>
                        <div class="metric">
                            <strong>03</strong>
                            <span>Consistent layouts across pages</span>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <footer>
            &copy; 2026 URL Shortener. Inspired by a Nintendo-style console chrome aesthetic.
        </footer>
    </div>
</body>
</html>
