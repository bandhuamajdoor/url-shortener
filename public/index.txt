<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Home - URL Shortener</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        :root {
            --primary: #e60012;
            --signal: #f68d1f;
            --amber: #ecab37;
            --nav-gold: #e48600;
            --canvas: #7a8aba;
            --canvas-soft: #9fbee7;
            --periwinkle: #8ba1d4;
            --chrome-indigo: #3d4f97;
            --muted-indigo: #60619c;
            --platinum: #dedede;
            --surface: #ffffff;
            --carbon: #21242e;
            --ink: #21242e;
            --ink-soft: #3d4f97;
            --on-primary: #ffffff;
            --shadow: rgba(33, 36, 46, 0.22);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
            color: var(--ink);
            background:
                radial-gradient(circle at top left, rgba(255, 255, 255, 0.34), transparent 35%),
                linear-gradient(180deg, #9cb2dc 0%, var(--canvas) 22%, #8d9fcd 58%, #7387b8 100%);
            min-height: 100vh;
        }

        a {
            color: inherit;
            text-decoration: none;
        }

        .shell {
            width: min(960px, calc(100% - 24px));
            margin: 0 auto;
            padding: 12px 0 20px;
        }

        .topbar {
            background: var(--carbon);
            color: var(--nav-gold);
            border: 1px solid rgba(255, 255, 255, 0.08);
            box-shadow: 0 10px 24px var(--shadow);
            padding: 10px 14px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 14px;
            flex-wrap: wrap;
        }

        .brand {
            display: inline-flex;
            align-items: center;
            gap: 10px;
        }

        .brand-badge {
            background: var(--surface);
            color: var(--primary);
            border-radius: 9999px;
            padding: 8px 14px;
            font-weight: 900;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            box-shadow: inset 0 1px 0 rgba(255,255,255,0.9), inset 0 -2px 0 rgba(61, 79, 151, 0.15);
        }

        .brand-copy {
            color: var(--on-primary);
            font-size: 12px;
            line-height: 1.3;
        }

        .nav {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        .nav a {
            color: var(--nav-gold);
        }

        .nav a:hover {
            color: var(--signal);
        }

        .subbar {
            background: linear-gradient(180deg, #b0c8eb, var(--canvas-soft));
            color: var(--ink);
            border-left: 1px solid rgba(255,255,255,0.4);
            border-right: 1px solid rgba(33, 36, 46, 0.15);
            border-bottom: 1px solid rgba(61, 79, 151, 0.4);
            padding: 8px 14px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
        }

        .hero {
            margin-top: 16px;
            background:
                linear-gradient(135deg, rgba(255,255,255,0.18), rgba(255,255,255,0.04)),
                linear-gradient(180deg, #aab9e4, var(--lavender, #acace7));
            border: 1px solid rgba(61, 79, 151, 0.5);
            box-shadow: 0 14px 30px var(--shadow);
            padding: 22px;
            position: relative;
            overflow: hidden;
        }

        .hero::after {
            content: "";
            position: absolute;
            inset: auto -30px -32px auto;
            width: 180px;
            height: 180px;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(246,141,31,0.38), transparent 70%);
            pointer-events: none;
        }

        .hero-grid {
            display: grid;
            grid-template-columns: 1.2fr 0.8fr;
            gap: 18px;
            align-items: stretch;
        }

        .panel {
            background: rgba(255,255,255,0.9);
            border: 1px solid rgba(61, 79, 151, 0.34);
            box-shadow: inset 0 1px 0 rgba(255,255,255,0.8);
        }

        .intro {
            padding: 18px;
            border-radius: 4px;
        }

        .eyebrow {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: var(--carbon);
            color: var(--canvas-soft);
            padding: 5px 10px;
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
        }

        h1 {
            margin: 14px 0 10px;
            font-size: clamp(2rem, 4vw, 3.8rem);
            line-height: 0.96;
            letter-spacing: -0.04em;
            text-transform: uppercase;
            color: #ffffff;
            text-shadow:
                -3px -3px 0 var(--carbon),
                3px -3px 0 var(--carbon),
                -3px 3px 0 var(--carbon),
                3px 3px 0 var(--carbon),
                0 6px 0 rgba(33, 36, 46, 0.55);
        }

        .lead {
            margin: 0;
            max-width: 56ch;
            font-size: 14px;
            line-height: 1.55;
            color: #24304b;
        }

        .stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
            margin-top: 16px;
        }

        .stat {
            background: linear-gradient(180deg, #fff, #f0f2fa);
            border: 1px solid rgba(61, 79, 151, 0.24);
            border-radius: 4px;
            padding: 12px;
        }

        .stat strong {
            display: block;
            color: var(--primary);
            font-size: 20px;
            line-height: 1;
        }

        .stat span {
            display: block;
            margin-top: 6px;
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            font-weight: 700;
            color: var(--ink-soft);
        }

        .form-card {
            padding: 18px;
            border-radius: 4px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            gap: 12px;
            background: linear-gradient(180deg, #f4f4f7, var(--platinum));
        }

        .form-card h2 {
            margin: 0;
            font-size: 18px;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            color: var(--carbon);
        }

        .form-card p {
            margin: 0;
            font-size: 13px;
            line-height: 1.5;
            color: #394968;
        }

        form {
            display: grid;
            gap: 12px;
            margin-top: 4px;
        }

        input[type="url"] {
            width: 100%;
            border: 1px solid rgba(61, 79, 151, 0.45);
            border-top-color: rgba(255,255,255,0.9);
            background: #fff;
            border-radius: 4px;
            padding: 14px 14px;
            font-size: 14px;
            color: var(--ink);
            box-shadow: inset 0 1px 2px rgba(33, 36, 46, 0.08);
        }

        input[type="url"]:focus {
            outline: 3px solid rgba(246, 141, 31, 0.28);
            border-color: var(--signal);
        }

        .actions {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            align-items: center;
        }

        button,
        .chip {
            appearance: none;
            border: 0;
            cursor: pointer;
            font: inherit;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            border-radius: 4px;
        }

        .button-primary {
            background: linear-gradient(180deg, #f0bc53, var(--amber));
            color: var(--carbon);
            padding: 14px 18px;
            box-shadow: inset 0 1px 0 rgba(255,255,255,0.7), 0 6px 0 rgba(138, 98, 12, 0.28);
        }

        .button-primary:hover {
            transform: translateY(-1px);
        }

        .chip {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 12px 16px;
            background: var(--carbon);
            color: var(--on-primary);
        }

        .feature-strip {
            margin-top: 16px;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 12px;
        }

        .feature {
            background: linear-gradient(180deg, #fff, #eef2fb);
            border: 1px solid rgba(61, 79, 151, 0.24);
            padding: 14px;
            border-radius: 4px;
        }

        .feature .label {
            display: inline-block;
            margin-bottom: 8px;
            padding: 4px 8px;
            background: var(--canvas-soft);
            color: var(--carbon);
            font-size: 10px;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
        }

        .feature h3 {
            margin: 0 0 8px;
            font-size: 14px;
            color: var(--ink);
            text-transform: uppercase;
            letter-spacing: 0.06em;
        }

        .feature p {
            margin: 0;
            font-size: 12px;
            line-height: 1.5;
            color: #384867;
        }

        footer {
            margin-top: 16px;
            background: var(--carbon);
            color: var(--canvas-soft);
            font-size: 10px;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            padding: 14px 16px;
            box-shadow: 0 10px 24px var(--shadow);
        }

        @media (max-width: 820px) {
            .hero-grid,
            .feature-strip,
            .stats {
                grid-template-columns: 1fr;
            }

            .shell {
                width: min(100% - 16px, 960px);
            }
        }
    </style>
</head>
<body>
    <div class="shell">
        <header class="topbar">
            <div class="brand">
                <div class="brand-badge">URL Shortener</div>
                <div class="brand-copy">Brushed chrome links with a fast, shareable redirect flow.</div>
            </div>
            <nav class="nav" aria-label="Primary">
                <a href="/index">Home</a>
                <a href="/about">About</a>
                <a href="/contact">Contact</a>
            </nav>
        </header>

        <div class="subbar">
            <span>Fast redirects</span>
            <span>Persistent JSON storage</span>
            <span>Clipboard-ready results</span>
        </div>

        <main class="hero">
            <div class="hero-grid">
                <section class="intro panel">
                    <div class="eyebrow">Command center</div>
                    <h1>Shorten links with a sharper interface</h1>
                    <p class="lead">Turn long URLs into compact, easy-to-share links. The result page is designed to surface the short link clearly, with a single-click copy action and a clean, high-contrast chrome-inspired layout.</p>

                    <div class="stats" aria-label="Highlights">
                        <div class="stat">
                            <strong>1</strong>
                            <span>Paste URL</span>
                        </div>
                        <div class="stat">
                            <strong>2</strong>
                            <span>Generate code</span>
                        </div>
                        <div class="stat">
                            <strong>3</strong>
                            <span>Share instantly</span>
                        </div>
                    </div>
                </section>

                <section class="form-card panel" aria-label="Shorten URL form">
                    <div class="eyebrow">Create link</div>
                    <h2>Shorten a URL</h2>
                    <p>Paste the destination address below, then press the amber button to generate a new short code.</p>
                    <form action="/shorten" method="POST">
                        <input type="url" name="url" placeholder="https://example.com/very/long/link" required>
                        <div class="actions">
                            <button class="button-primary" type="submit">Shorten URL</button>
                            <a class="chip" href="/about">Learn More</a>
                        </div>
                    </form>
                </section>
            </div>
        </main>

        <section class="feature-strip" aria-label="Features">
            <article class="feature">
                <span class="label">Persistent</span>
                <h3>Saved to JSON</h3>
                <p>Every mapping is written to disk so links stay available after restart.</p>
            </article>
            <article class="feature">
                <span class="label">Direct</span>
                <h3>Low overhead routing</h3>
                <p>The server handles requests directly over TCP without extra framework layers.</p>
            </article>
            <article class="feature">
                <span class="label">Fast</span>
                <h3>Copy and go</h3>
                <p>The result page presents the short link clearly for quick clipboard access.</p>
            </article>
        </section>

        <footer>
            &copy; 2026 URL Shortener. Built with Rust and a console-chrome inspired interface.
        </footer>
    </div>
</body>
</html>
