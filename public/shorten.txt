<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Shortened URL</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        :root {
            --primary: #e60012;
            --signal: #f68d1f;
            --amber: #ecab37;
            --canvas: #7a8aba;
            --canvas-soft: #9fbee7;
            --periwinkle: #8ba1d4;
            --chrome-indigo: #3d4f97;
            --platinum: #dedede;
            --surface: #ffffff;
            --carbon: #21242e;
            --ink: #21242e;
            --ink-soft: #3d4f97;
            --shadow: rgba(33, 36, 46, 0.22);
        }

        * { box-sizing: border-box; }

        body {
            margin: 0;
            min-height: 100vh;
            font-family: Arial, Helvetica, sans-serif;
            color: var(--ink);
            background:
                radial-gradient(circle at top left, rgba(255, 255, 255, 0.34), transparent 35%),
                linear-gradient(180deg, #9cb2dc 0%, var(--canvas) 22%, #8d9fcd 58%, #7387b8 100%);
            display: grid;
            place-items: center;
            padding: 20px 12px;
        }

        .card {
            width: min(860px, 100%);
            background: linear-gradient(180deg, rgba(255,255,255,0.92), rgba(238,242,251,0.96));
            border: 1px solid rgba(61, 79, 151, 0.4);
            box-shadow: 0 16px 32px var(--shadow);
            overflow: hidden;
        }

        .topbar {
            background: var(--carbon);
            color: #fff;
            padding: 12px 16px;
            display: flex;
            justify-content: space-between;
            gap: 10px;
            align-items: center;
            flex-wrap: wrap;
        }

        .topbar strong {
            background: var(--surface);
            color: var(--primary);
            border-radius: 9999px;
            padding: 6px 12px;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        .topbar span {
            font-size: 11px;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            color: var(--canvas-soft);
        }

        .body {
            padding: 24px;
        }

        .eyebrow {
            display: inline-flex;
            background: var(--carbon);
            color: var(--canvas-soft);
            padding: 5px 10px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        h1 {
            margin: 14px 0 8px;
            font-size: clamp(2rem, 4vw, 3.2rem);
            line-height: 0.95;
            text-transform: uppercase;
            color: #fff;
            text-shadow:
                -3px -3px 0 var(--carbon),
                3px -3px 0 var(--carbon),
                -3px 3px 0 var(--carbon),
                3px 3px 0 var(--carbon),
                0 6px 0 rgba(33, 36, 46, 0.55);
        }

        .lede {
            margin: 0;
            color: #394968;
            line-height: 1.5;
            font-size: 14px;
            max-width: 62ch;
        }

        .result-grid {
            display: grid;
            grid-template-columns: minmax(0, 1fr) auto;
            gap: 12px;
            margin-top: 18px;
            align-items: center;
        }

        .short-url-box {
            background: linear-gradient(180deg, #fff, #f1f4fb);
            border: 1px solid rgba(61, 79, 151, 0.3);
            border-radius: 4px;
            padding: 18px;
            box-shadow: inset 0 1px 0 rgba(255,255,255,0.85);
        }

        .short-url-label {
            display: block;
            margin-bottom: 8px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            color: var(--ink-soft);
        }

        .short-url {
            word-break: break-all;
            font-size: clamp(1rem, 2vw, 1.35rem);
            font-weight: 700;
            color: var(--carbon);
        }

        .copy-btn,
        .back-link {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            border: 0;
            border-radius: 4px;
            cursor: pointer;
            font: inherit;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            text-decoration: none;
            white-space: nowrap;
        }

        .copy-btn {
            background: linear-gradient(180deg, #f3be53, var(--amber));
            color: var(--carbon);
            padding: 14px 18px;
            box-shadow: inset 0 1px 0 rgba(255,255,255,0.7), 0 6px 0 rgba(138, 98, 12, 0.24);
        }

        .copy-btn:hover,
        .back-link:hover {
            transform: translateY(-1px);
        }

        .back-link {
            margin-top: 18px;
            background: var(--carbon);
            color: #fff;
            padding: 12px 16px;
        }

        #copied-msg {
            margin-top: 14px;
            display: none;
            color: #184e2b;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        .notes {
            margin-top: 18px;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 12px;
        }

        .note {
            background: rgba(255,255,255,0.8);
            border: 1px solid rgba(61, 79, 151, 0.24);
            border-radius: 4px;
            padding: 12px;
        }

        .note strong {
            display: block;
            color: var(--primary);
            text-transform: uppercase;
            font-size: 11px;
            letter-spacing: 0.08em;
            margin-bottom: 6px;
        }

        .note p {
            margin: 0;
            font-size: 12px;
            line-height: 1.45;
            color: #394968;
        }

        @media (max-width: 760px) {
            .result-grid,
            .notes {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <section class="card" aria-label="Shortened URL result">
        <header class="topbar">
            <strong>URL Shortener</strong>
            <span>Result ready for copy and share</span>
        </header>

        <div class="body">
            <div class="eyebrow">Output panel</div>
            <h1>Your shortened link</h1>
            <p class="lede">The generated URL below is the shareable version. Copy it once and use it wherever you need a shorter, cleaner link.</p>

            <div class="result-grid">
                <div class="short-url-box">
                    <span class="short-url-label">Short URL</span>
                    <span id="shortUrl" class="short-url">{short_url}</span>
                </div>
                <button class="copy-btn" onclick="copyToClipboard()">Copy Link</button>
            </div>

            <div id="copied-msg">Copied to clipboard!</div>

            <div class="notes">
                <div class="note">
                    <strong>Direct</strong>
                    <p>The browser redirects to the original destination as soon as the code is requested.</p>
                </div>
                <div class="note">
                    <strong>Persistent</strong>
                    <p>Your link mapping is stored in JSON so it survives server restarts.</p>
                </div>
                <div class="note">
                    <strong>Ready</strong>
                    <p>Use the button to copy the new link, or return home to shorten another address.</p>
                </div>
            </div>

            <a class="back-link" href="/index">Shorten another URL</a>
        </div>
    </section>

    <script>
        function copyToClipboard() {
            const url = document.getElementById("shortUrl").textContent;
            navigator.clipboard.writeText(url).then(() => {
                const msg = document.getElementById("copied-msg");
                msg.style.display = "block";
                setTimeout(() => {
                    msg.style.display = "none";
                }, 2000);
            });
        }
    </script>
</body>
</html>
